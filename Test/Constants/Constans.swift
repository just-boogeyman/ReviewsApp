import UIKit

enum ConstansApp {
	enum API {
		static let responseFileName = "getReviews.response"
		static let withExtension = "json"
	}
	
	enum Placeholder {
		/// Возвращает картинку UIImage "avatar"
		static let avatar = UIImage(named: "avatar")
		/// Возвращает текст "Обновление..."
		static let update = "Обновление..."
	}

}
